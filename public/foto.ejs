<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar Foto</title>
</head>
<body>
    <h1>Daftar Foto</h1>
    <ul>
        <% fotoList.forEach(foto => { %>
            <li>
                <img src="/foto/<%= foto %>" alt="<%= foto %>" style="width: 200px; height: auto;">
                <p><%= foto %></p>
            </li>
        <% }) %>
    </ul>
</body>
</html>